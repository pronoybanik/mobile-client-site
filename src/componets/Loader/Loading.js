import React from 'react';

const Loading = () => {
    return (
        <div>
            <button className="btn btn-outline loading flex justify-center items-center">loading</button>
        </div>
    );
};

export default Loading;